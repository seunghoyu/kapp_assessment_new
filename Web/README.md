# Web — Next.js 앱

이 폴더가 Next.js 16 프로젝트 루트입니다.

```bash
npm install
npm run dev
```

저장소 루트에서 실행하려면: `npm run dev` (루트 `package.json`이 `--prefix Web`으로 위임).

루트의 `scripts/` 유틸은 `../scripts/...` 경로로 연결되어 있습니다 (`package.json`의 `docs` 스크립트).
