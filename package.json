{
  "name": "kapp_assessment_new",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo-style root: Next app lives in Web/. Run npm scripts from repo root or cd Web.",
  "scripts": {
    "dev": "npm run dev --prefix Web",
    "build": "npm run build --prefix Web",
    "start": "npm run start --prefix Web",
    "lint": "npm run lint --prefix Web",
    "install:web": "npm install --prefix Web",
    "docs": "npm run docs --prefix Web",
    "build:docs-diagram": "npm run build:docs-diagram --prefix Web"
  }
}
